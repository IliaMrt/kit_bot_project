version: "3.3"

services:

  bot:
    container_name: bot
    build:
      context: ./kit_bot
      dockerfile: ./Dockerfile
    restart: always
    network_mode: host

  feedback:
    container_name: feedback
    build:
      context: ./kit_feedback_collection_for_bot
      dockerfile: ./Dockerfile
    restart: always
    network_mode: host
